# tifa-widget

Frontend widget boundary for TifaWidget and future reusable React widget work:

- client event contracts
- widget props
- future React hooks
- future package extraction

The current reference UI remains `components/TifaWidget.tsx`; this module is a typed boundary and does not redesign the widget.
