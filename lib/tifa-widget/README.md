# tifa-widget

Frontend widget boundary for ChatTifa and future reusable React widget work:

- client event contracts
- widget props
- future React hooks
- future package extraction

The current reference UI remains `components/ChatTifa.tsx`; this module is a typed boundary and does not redesign the widget.

